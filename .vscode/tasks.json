{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "UID=${UID} GID=${GID} docker-compose run --rm --entrypoint '/bin/bash -c \"cd /app/paraview-build && rm -f superbuild/paraview/stamp/paraview-build && make\"' paraview_dev",
            "problemMatcher": {
                "owner": "cpp",
                "fileLocation": ["relative", "${workspaceFolder}/build/"],
                "pattern": {
                  "regexp": "^/app/paraview-build/(.*):(\\d+):(\\d+):\\s+(warning|error):\\s+(.*)$",
                  "file": 1,
                  "line": 2,
                  "column": 3,
                  "severity": 4,
                  "message": 5
                },
                "source": "gcc-docker"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Launch",
            "type": "shell",
            "command": "UID=${UID} GID=${GID} docker-compose run --rm --entrypoint '/bin/bash -c \"cd /app/paraview-build && ./install/bin/paraview\"' paraview_dev",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        },
        {
            "label": "Launch-Debugserver",
            "type": "shell",
            "command": "UID=${UID} GID=${GID} docker-compose run --rm -d --entrypoint '/bin/bash -c \"cd /app/paraview-build && gdbserver :2000 ./install/bin/paraview\"' paraview_dev",
            "group": "test"
        }
    ]
}